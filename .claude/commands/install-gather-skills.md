Install gather engine + requirements, architecture, and harvesting skills globally.

Run:
```bash
npx skills add . -g -a cursor claude-code -s gather -s propose-requirements -s validate-requirements -s propose-architecture -s validate-architecture -s project-architecture -s project-requirements -s harvest-patterns -y
```

Report which skills were installed.
