Install gather engine + all requirements and architecture skills globally.

Run:
```bash
npx skills add . -g -a cursor claude-code -s gather -s propose-requirements -s validate-requirements -s propose-architecture -s validate-architecture -y
```

Report which skills were installed.
